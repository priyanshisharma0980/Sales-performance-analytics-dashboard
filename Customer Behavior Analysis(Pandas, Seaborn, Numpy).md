### OBJECTIVE
I analyzed around 300,000 customer transaction records to understand customer purchasing behavior.  
First, I extracted transaction data from a SQL database. Then I used Python (Pandas and NumPy) to clean and process the data.  
I performed exploratory data analysis to understand spending patterns, purchase frequency, and product category trends.  
Then I created customer segments based on total spending and identified high-value customers.  


### objective was to identify high-value customers and understand their purchasing behavior

### IMP QUESTIONS
Who are valuable customers?	Calculate total spend per customer   
Who buys frequently?	Count orders per customer  
Which products generate revenue?	Analyze category sales   




### What kind of data did you work with?
The dataset contained customer transaction records.
Main columns included:  
customer_id  
order_id   
product_category  
order_value  
purchase_date  
city  
Each row represented one purchase made by a customer.    




### What data cleaning steps did you perform?
I performed several cleaning steps:
Removed duplicate transaction records
Handled missing values in order_value
Converted purchase_date to datetime format
Checked for invalid or negative order values

### Find which customers spend the mostand understand their behavior
We want to understand customer purchasing behavior
“We will analyze transaction data and segment customers.”

Who are our high-value customers?
Which customers purchase frequently?


### How did you calculate customer spending?
I grouped the dataset by customer_id and calculated the total amount each customer spent.
customer_spend = df.groupby('customer_id')['order_value'].sum()
This gave the total spending per customer, which was used for segmentation.

### How did you calculate that high-value customers were 18% of users?
First I counted total number of customers.
Then I counted customers belonging to the high-value segment.
Finally I calculated:
(high_value_customers / total_customers) × 100
This gave approximately 18% of customers.

### How did you calculate that they generate 62% revenue?
Total revenue from all customers
Revenue generated by high-value customer
Then I computed:
(high_value_revenue / total_revenue) × 100
This showed that high-value customers contributed around 62% of total revenue.

### Why did you choose ₹10,000 as the threshold for high-value customers?
I did not pick the number randomly.
First, I analyzed the distribution of total customer spending using a histogram.
I noticed that most customers spent below ₹3,000, while a smaller group spent above ₹10,000.



### Did you check for outliers in the dataset?
Yes, I checked for extreme order values using boxplots.
There were a few unusually large transactions, but they represented legitimate high purchases.
Instead of removing them, I kept them because they reflect actual high-value customers.



### METRICS
| Metric              | Description                    |
| ------------------- | ------------------------------ |
| Total Spend         | Total money spent per customer |
| Purchase Frequency  | Number of orders               |
| Average Order Value | Spend per order                |


TABLES
| Column           | Description        |
| ---------------- | ------------------ |
| customer_id      | unique user        |
| order_id         | order number       |
| product_category | product type       |
| order_value      | transaction amount |
| purchase_date    | purchase time      |


### Setup a connection between SQL server and Python
import pandas as pd
import pyodbc
conn = pyodbc.connect(
    "DRIVER={SQL Server};"
    "SERVER=companyserver.database.windows.net;"
    "DATABASE=salesdb;"
    "UID=username;"
    "PWD=password"
)

df = pd.read_sql(query, conn)

### Understand the data
df.shape    --- gives rows and columns
df.info()   
It gives column names
data types
missing values


### Fixing Data Types
df['purchase_date'] = pd.to_datetime(df['purchase_date'])


### REMOVE DUPLICATES
df = df.drop_duplicates()

### Handling Missing Data
Option 1 remove rows
df = df.dropna()

Option 2 fill values
df['price'] = df['price'].fillna(df['price'].median())


### Creating Business Metrics
Total spending per customer
customer_spend = df.groupby('customer_id')['price'].sum()


### Creating Customer Behavior Features
frequency = df.groupby('customer_id')['order_id'].count()

### HEAT MAPS CORR
Do frequent buyers spend more money?
sns.heatmap(df.corr())

### Identifying High Value Customers

Segment	Spend
High value	>10000
Medium	3000–10000
Low	<3000

df['segment'] = pd.cut(
df['total_spend'],
bins=[0,3000,10000,50000],
labels=['Low','Medium','High']
)

Count High Value Customers
high_value_customers = customer_data[customer_data['segment']=='High']

Calculate Revenue From High Value Customers
high_value_revenue = high_value_customers['total_spend'].sum()

### Most purchases between ₹500–₹3000
import seaborn as sns  
import matplotlib.pyplot as plt  

sns.histplot(df['order_value'])  
plt.show()  


### Revenue by product category
category_sales = df.groupby('product_category')['order_value'].sum()  
















































